<!-- Modern Funky Banner with Full-Page Background -->
<section id="modern-hero" class="modern-hero-section hero-section-modern position-relative overflow-hidden">
    <!-- Animated Background Elements -->
    <div class="hero-bg-modern"></div>

    <!-- Funky Geometric Shapes -->
    <div class="floating-shapes">
        <div class="shape shape-1">
            <div class="shape-inner gradient-1"></div>
        </div>
        <div class="shape shape-2">
            <div class="shape-inner gradient-2"></div>
        </div>
        <div class="shape shape-3">
            <div class="shape-inner gradient-3"></div>
        </div>
        <div class="shape shape-4">
            <div class="shape-inner gradient-4"></div>
        </div>
        <div class="shape shape-5">
            <div class="shape-inner gradient-5"></div>
        </div>
    </div>

    <!-- Particle System -->
    <div class="particles">
        <div class="particle particle-1"></div>
        <div class="particle particle-2"></div>
        <div class="particle particle-3"></div>
        <div class="particle particle-4"></div>
        <div class="particle particle-5"></div>
        <div class="particle particle-6"></div>
        <div class="particle particle-7"></div>
        <div class="particle particle-8"></div>
    </div>

    <!-- Grid Overlay -->
    <div class="grid-overlay"></div>

    <div class="container">
        <div class="row align-items-center min-vh-100 py-5">
            <!-- Content Column -->
            <div class="col-lg-6" data-aos="fade-right" data-aos-duration="1200">
                <div class="hero-content-modern">
                    <!-- Animated Badge -->
                    <div class="modern-badge" data-aos="zoom-in" data-aos-delay="300">
                        <span class="badge-text">
                            <i class="fas fa-bolt me-2"></i>
                            Next-Gen Event Management
                        </span>
                        <div class="badge-glow"></div>
                    </div>

                    <!-- Main Title with Typing Effect -->
                    <h1 class="modern-title" data-aos="fade-up" data-aos-delay="500">
                        <span class="title-line">We Create</span>
                        <span class="title-line gradient-text">
                            <span class="typed-text" data-typed='["Extraordinary Events", "Digital Experiences", "Brand Stories", "Memorable Moments", "Future-Ready Solutions"]'></span>
                            <span class="cursor">|</span>
                        </span>
                        <span class="title-line">That Matter</span>
                    </h1>

                    <!-- Subtitle -->
                    <p class="modern-subtitle" data-aos="fade-up" data-aos-delay="700">
                        Pushing boundaries in event management with cutting-edge technology,
                        creative innovation, and data-driven strategies that deliver exceptional results.
                    </p>

                    <!-- Modern Stats -->
                    <div class="modern-stats" data-aos="fade-up" data-aos-delay="900">
                        <div class="stat-item">
                            <div class="stat-number" data-count="500">0</div>
                            <div class="stat-label">Events Created</div>
                        </div>
                        <div class="stat-divider"></div>
                        <div class="stat-item">
                            <div class="stat-number" data-count="98">0</div>
                            <div class="stat-label">Client Satisfaction</div>
                        </div>
                        <div class="stat-divider"></div>
                        <div class="stat-item">
                            <div class="stat-number" data-count="15">0</div>
                            <div class="stat-label">Years Experience</div>
                        </div>
                    </div>

                    <!-- CTA Buttons -->
                    <div class="hero-actions-modern" data-aos="fade-up" data-aos-delay="1100">
                        <a href="<?php echo home_url('/contact'); ?>" class="btn btn-modern-primary btn-lg">
                            <span class="btn-text">Start Your Project</span>
                            <span class="btn-icon">
                                <i class="fas fa-rocket"></i>
                            </span>
                        </a>
                        <a href="<?php echo home_url('/portfolio'); ?>" class="btn btn-modern-outline btn-lg">
                            <span class="btn-text">View Portfolio</span>
                            <span class="btn-icon">
                                <i class="fas fa-play"></i>
                            </span>
                        </a>
                    </div>
                </div>
            </div>

            <!-- Visual Column -->
            <div class="col-lg-6" data-aos="fade-left" data-aos-duration="1200" data-aos-delay="400">
                <div class="hero-visual-modern position-relative">
                    <!-- 3D Card Stack -->
                    <div class="card-stack">
                        <div class="hero-card card-1" data-aos="flip-left" data-aos-delay="800">
                            <div class="card-content">
                                <div class="card-icon">
                                    <i class="fas fa-calendar-alt"></i>
                                </div>
                                <h4>Corporate Events</h4>
                                <p>Premium corporate event management solutions</p>
                            </div>
                            <div class="card-glow glow-1"></div>
                        </div>

                        <div class="hero-card card-2" data-aos="flip-left" data-aos-delay="1000">
                            <div class="card-content">
                                <div class="card-icon">
                                    <i class="fas fa-video"></i>
                                </div>
                                <h4>Virtual Experiences</h4>
                                <p>Cutting-edge virtual and hybrid events</p>
                            </div>
                            <div class="card-glow glow-2"></div>
                        </div>

                        <div class="hero-card card-3" data-aos="flip-left" data-aos-delay="1200">
                            <div class="card-content">
                                <div class="card-icon">
                                    <i class="fas fa-bullhorn"></i>
                                </div>
                                <h4>Brand Activation</h4>
                                <p>Innovative brand communication strategies</p>
                            </div>
                            <div class="card-glow glow-3"></div>
                        </div>
                    </div>

                    <!-- Floating Elements -->
                    <div class="floating-elements">
                        <div class="floating-icon icon-1" data-aos="fade-up" data-aos-delay="1400">
                            <i class="fas fa-users"></i>
                        </div>
                        <div class="floating-icon icon-2" data-aos="fade-up" data-aos-delay="1600">
                            <i class="fas fa-chart-line"></i>
                        </div>
                        <div class="floating-icon icon-3" data-aos="fade-up" data-aos-delay="1800">
                            <i class="fas fa-trophy"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Scroll Indicator with Animation -->
    <div class="scroll-indicator-modern">
        <div class="scroll-text">Scroll to explore</div>
        <div class="scroll-arrow">
            <i class="fas fa-chevron-down"></i>
        </div>
    </div>
</section>

<!-- Modern Dark Styles -->
<style>
    :root {
        --modern-primary: #00d4ff;
        --modern-secondary: #ff6b35;
        --modern-accent: #ff0080;
        --modern-success: #00ff88;
        --modern-warning: #ffaa00;
        --modern-dark: #0a0a0a;
        --modern-darker: #050505;
        --modern-gray: #1a1a1a;
        --modern-light-gray: #333;
        --modern-text: #ffffff;
        --modern-text-muted: #a0a0a0;
    }

    .modern-hero-section {
        background: linear-gradient(135deg, var(--modern-darker) 0%, var(--modern-dark) 100%);
        min-height: 100vh;
        position: relative;
        padding: 120px 0 80px;
        color: var(--modern-text);
        overflow: hidden;
    }

    /* Animated Background */
    .hero-bg-modern::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background:
            radial-gradient(circle at 20% 80%, rgba(0, 212, 255, 0.1) 0%, transparent 50%),
            radial-gradient(circle at 80% 20%, rgba(255, 107, 53, 0.1) 0%, transparent 50%),
            radial-gradient(circle at 40% 40%, rgba(255, 0, 128, 0.05) 0%, transparent 50%);
        animation: backgroundPulse 6s ease-in-out infinite alternate;
    }

    @keyframes backgroundPulse {
        0% {
            opacity: 0.5;
            transform: scale(1);
        }

        100% {
            opacity: 0.8;
            transform: scale(1.05);
        }
    }

    /* Grid Overlay */
    .grid-overlay {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background-image:
            linear-gradient(rgba(255, 255, 255, 0.02) 1px, transparent 1px),
            linear-gradient(90deg, rgba(255, 255, 255, 0.02) 1px, transparent 1px);
        background-size: 50px 50px;
        animation: gridMove 20s linear infinite;
    }

    @keyframes gridMove {
        0% {
            transform: translate(0, 0);
        }

        100% {
            transform: translate(50px, 50px);
        }
    }

    /* Floating Shapes */
    .floating-shapes {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        overflow: hidden;
        z-index: 1;
    }

    .shape {
        position: absolute;
        border-radius: 50%;
        animation: floatRotate 8s ease-in-out infinite;
    }

    .shape-inner {
        width: 100%;
        height: 100%;
        border-radius: 50%;
        filter: blur(1px);
    }

    .gradient-1 {
        background: linear-gradient(45deg, var(--modern-primary), transparent);
    }

    .gradient-2 {
        background: linear-gradient(45deg, var(--modern-secondary), transparent);
    }

    .gradient-3 {
        background: linear-gradient(45deg, var(--modern-accent), transparent);
    }

    .gradient-4 {
        background: linear-gradient(45deg, var(--modern-success), transparent);
    }

    .gradient-5 {
        background: linear-gradient(45deg, var(--modern-warning), transparent);
    }

    .shape-1 {
        width: 200px;
        height: 200px;
        top: 10%;
        right: 10%;
        animation-delay: 0s;
    }

    .shape-2 {
        width: 150px;
        height: 150px;
        bottom: 20%;
        left: 5%;
        animation-delay: 2s;
    }

    .shape-3 {
        width: 100px;
        height: 100px;
        top: 60%;
        right: 30%;
        animation-delay: 4s;
    }

    .shape-4 {
        width: 80px;
        height: 80px;
        top: 30%;
        left: 20%;
        animation-delay: 1s;
    }

    .shape-5 {
        width: 120px;
        height: 120px;
        bottom: 40%;
        right: 60%;
        animation-delay: 3s;
    }

    @keyframes floatRotate {

        0%,
        100% {
            transform: translateY(0) rotate(0deg);
            opacity: 0.3;
        }

        25% {
            transform: translateY(-20px) rotate(90deg);
            opacity: 0.6;
        }

        50% {
            transform: translateY(-10px) rotate(180deg);
            opacity: 0.4;
        }

        75% {
            transform: translateY(-30px) rotate(270deg);
            opacity: 0.7;
        }
    }

    /* Particles */
    .particles {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        overflow: hidden;
    }

    .particle {
        position: absolute;
        width: 4px;
        height: 4px;
        background: var(--modern-primary);
        border-radius: 50%;
        animation: particleFloat 6s ease-in-out infinite;
    }

    .particle-1 {
        top: 10%;
        left: 10%;
        animation-delay: 0s;
    }

    .particle-2 {
        top: 20%;
        left: 80%;
        animation-delay: 1s;
    }

    .particle-3 {
        top: 70%;
        left: 20%;
        animation-delay: 2s;
    }

    .particle-4 {
        top: 80%;
        left: 70%;
        animation-delay: 3s;
    }

    .particle-5 {
        top: 40%;
        left: 60%;
        animation-delay: 0.5s;
    }

    .particle-6 {
        top: 60%;
        left: 40%;
        animation-delay: 1.5s;
    }

    .particle-7 {
        top: 30%;
        left: 90%;
        animation-delay: 2.5s;
    }

    .particle-8 {
        top: 90%;
        left: 10%;
        animation-delay: 3.5s;
    }

    @keyframes particleFloat {

        0%,
        100% {
            transform: translateY(0);
            opacity: 0.3;
        }

        50% {
            transform: translateY(-50px);
            opacity: 1;
        }
    }

    /* Hero Content */
    .hero-content-modern {
        position: relative;
        z-index: 2;
    }

    .modern-badge {
        position: relative;
        display: inline-block;
        margin-bottom: 2rem;
    }

    .badge-text {
        display: inline-block;
        background: rgba(0, 212, 255, 0.1);
        color: var(--modern-primary);
        padding: 12px 24px;
        border-radius: 50px;
        border: 1px solid rgba(0, 212, 255, 0.3);
        font-weight: 600;
        font-size: 0.9rem;
        backdrop-filter: blur(10px);
        position: relative;
        z-index: 2;
    }

    .badge-glow {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 100%;
        height: 100%;
        background: var(--modern-primary);
        border-radius: 50px;
        opacity: 0.2;
        filter: blur(10px);
        animation: badgeGlow 2s ease-in-out infinite alternate;
    }

    @keyframes badgeGlow {
        0% {
            transform: translate(-50%, -50%) scale(0.8);
            opacity: 0.2;
        }

        100% {
            transform: translate(-50%, -50%) scale(1.1);
            opacity: 0.4;
        }
    }

    /* Modern Typography */
    .modern-title {
        font-size: clamp(3rem, 8vw, 5.5rem);
        font-weight: 800;
        line-height: 1.1;
        margin-bottom: 1.5rem;
        font-family: 'Inter', sans-serif;
    }

    .title-line {
        display: block;
        margin-bottom: 0.2em;
    }

    .gradient-text {
        background: linear-gradient(135deg, var(--modern-primary) 0%, var(--modern-accent) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .typed-text {
        display: inline-block;
        min-width: 300px;
    }

    .cursor {
        animation: blink 1s infinite;
        color: var(--modern-primary);
    }

    @keyframes blink {

        0%,
        50% {
            opacity: 1;
        }

        51%,
        100% {
            opacity: 0;
        }
    }

    .modern-subtitle {
        font-size: 1.3rem;
        line-height: 1.7;
        color: var(--modern-text-muted);
        margin-bottom: 3rem;
        max-width: 90%;
    }

    /* Modern Stats */
    .modern-stats {
        display: flex;
        align-items: center;
        margin-bottom: 3rem;
        gap: 2rem;
    }

    .stat-item {
        text-align: center;
    }

    .stat-number {
        font-size: 2.5rem;
        font-weight: 800;
        color: var(--modern-primary);
        line-height: 1;
        margin-bottom: 0.5rem;
    }

    .stat-label {
        font-size: 0.9rem;
        color: var(--modern-text-muted);
        font-weight: 500;
    }

    .stat-divider {
        width: 1px;
        height: 60px;
        background: linear-gradient(to bottom, transparent, var(--modern-light-gray), transparent);
    }

    /* Modern Buttons */
    .hero-actions-modern {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .btn-modern-primary,
    .btn-modern-outline {
        display: inline-flex;
        align-items: center;
        gap: 1rem;
        padding: 16px 32px;
        border-radius: 50px;
        font-weight: 600;
        text-decoration: none;
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
        border: none;
        font-size: 1.1rem;
    }

    .btn-modern-primary {
        background: linear-gradient(135deg, var(--modern-primary), var(--modern-accent));
        color: white;
        box-shadow: 0 10px 30px rgba(0, 212, 255, 0.3);
    }

    .btn-modern-primary:hover {
        transform: translateY(-3px);
        box-shadow: 0 15px 40px rgba(0, 212, 255, 0.4);
        color: white;
    }

    .btn-modern-outline {
        background: transparent;
        color: var(--modern-text);
        border: 2px solid var(--modern-light-gray);
    }

    .btn-modern-outline:hover {
        background: rgba(255, 255, 255, 0.05);
        border-color: var(--modern-primary);
        color: var(--modern-primary);
        transform: translateY(-3px);
    }

    .btn-icon {
        transition: transform 0.3s ease;
    }

    .btn-modern-primary:hover .btn-icon,
    .btn-modern-outline:hover .btn-icon {
        transform: translateX(5px);
    }

    /* Hero Visual */
    .hero-visual-modern {
        position: relative;
        height: 600px;
    }

    .card-stack {
        position: relative;
        width: 100%;
        height: 100%;
    }

    .hero-card {
        position: absolute;
        width: 280px;
        height: 180px;
        background: rgba(255, 255, 255, 0.02);
        backdrop-filter: blur(20px);
        border-radius: 20px;
        border: 1px solid rgba(255, 255, 255, 0.1);
        padding: 2rem;
        transition: all 0.4s ease;
        cursor: pointer;
    }

    .hero-card:hover {
        transform: translateY(-10px) rotateX(5deg);
        background: rgba(255, 255, 255, 0.05);
    }

    .card-1 {
        top: 20%;
        left: 0;
        z-index: 3;
    }

    .card-2 {
        top: 40%;
        left: 15%;
        z-index: 2;
    }

    .card-3 {
        top: 60%;
        left: 30%;
        z-index: 1;
    }

    .card-icon {
        width: 60px;
        height: 60px;
        background: linear-gradient(135deg, var(--modern-primary), var(--modern-accent));
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        color: white;
        margin-bottom: 1rem;
    }

    .hero-card h4 {
        font-size: 1.3rem;
        font-weight: 700;
        color: var(--modern-text);
        margin-bottom: 0.5rem;
    }

    .hero-card p {
        font-size: 0.9rem;
        color: var(--modern-text-muted);
        line-height: 1.5;
    }

    .card-glow {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 200%;
        height: 200%;
        border-radius: 50%;
        opacity: 0;
        filter: blur(20px);
        transition: opacity 0.4s ease;
    }

    .hero-card:hover .card-glow {
        opacity: 0.1;
    }

    .glow-1 {
        background: var(--modern-primary);
    }

    .glow-2 {
        background: var(--modern-secondary);
    }

    .glow-3 {
        background: var(--modern-accent);
    }

    /* Floating Icons */
    .floating-elements {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
    }

    .floating-icon {
        position: absolute;
        width: 60px;
        height: 60px;
        background: rgba(0, 212, 255, 0.1);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        color: var(--modern-primary);
        font-size: 1.5rem;
        border: 1px solid rgba(0, 212, 255, 0.3);
        animation: iconFloat 4s ease-in-out infinite;
    }

    .icon-1 {
        top: 10%;
        right: 20%;
        animation-delay: 0s;
    }

    .icon-2 {
        bottom: 30%;
        right: 10%;
        animation-delay: 1s;
    }

    .icon-3 {
        top: 50%;
        right: 50%;
        animation-delay: 2s;
    }

    @keyframes iconFloat {

        0%,
        100% {
            transform: translateY(0) rotate(0deg);
        }

        50% {
            transform: translateY(-15px) rotate(180deg);
        }
    }

    /* Scroll Indicator */
    .scroll-indicator-modern {
        position: absolute;
        bottom: 30px;
        left: 50%;
        transform: translateX(-50%);
        text-align: center;
        color: var(--modern-text-muted);
    }

    .scroll-text {
        font-size: 0.8rem;
        margin-bottom: 1rem;
        font-weight: 500;
    }

    .scroll-arrow {
        font-size: 1.2rem;
        animation: scrollBounce 2s ease-in-out infinite;
    }

    @keyframes scrollBounce {

        0%,
        100% {
            transform: translateY(0);
        }

        50% {
            transform: translateY(10px);
        }
    }

    /* Counter Animation */
    @keyframes countUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Responsive Design */
    @media (max-width: 991px) {
        .modern-hero-section {
            padding: 100px 0 60px;
        }

        .card-stack {
            height: 400px;
            margin-top: 3rem;
        }

        .hero-card {
            width: 240px;
            height: 160px;
            padding: 1.5rem;
        }

        .card-1,
        .card-2,
        .card-3 {
            position: static;
            margin-bottom: 1rem;
        }

        .floating-elements {
            display: none;
        }

        .modern-stats {
            justify-content: center;
        }
    }

    @media (max-width: 768px) {
        .modern-title {
            font-size: 2.5rem;
        }

        .modern-subtitle {
            font-size: 1.1rem;
            max-width: 100%;
        }

        .hero-actions-modern {
            justify-content: center;
        }

        .btn-modern-primary,
        .btn-modern-outline {
            width: 100%;
            justify-content: center;
            margin-bottom: 1rem;
        }

        .modern-stats {
            flex-direction: column;
            gap: 1rem;
        }

        .stat-divider {
            width: 100px;
            height: 1px;
        }
    }

    /* Modern Navigation Styles */
    .modern-dark-nav {
        background: rgba(10, 10, 10, 0.95) !important;
        backdrop-filter: blur(20px);
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        transition: all 0.3s ease;
    }

    .modern-logo {
        display: flex;
        align-items: center;
    }

    .logo-text {
        font-size: 1.8rem;
        font-weight: 800;
        background: linear-gradient(135deg, var(--modern-primary), var(--modern-accent));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .logo-dot {
        width: 8px;
        height: 8px;
        background: var(--modern-primary);
        border-radius: 50%;
        margin-left: 5px;
        animation: dotPulse 2s ease-in-out infinite;
    }

    @keyframes dotPulse {

        0%,
        100% {
            transform: scale(1);
            opacity: 1;
        }

        50% {
            transform: scale(1.5);
            opacity: 0.5;
        }
    }

    .navbar-nav .nav-link {
        color: var(--modern-text) !important;
        font-weight: 500;
        margin: 0 0.5rem;
        transition: all 0.3s ease;
        position: relative;
    }

    .navbar-nav .nav-link::after {
        content: '';
        position: absolute;
        bottom: -5px;
        left: 50%;
        width: 0;
        height: 2px;
        background: linear-gradient(135deg, var(--modern-primary), var(--modern-accent));
        transition: all 0.3s ease;
        transform: translateX(-50%);
    }

    .navbar-nav .nav-link:hover::after {
        width: 100%;
    }

    .navbar-nav .nav-link:hover {
        color: var(--modern-primary) !important;
    }

    .dark-dropdown {
        background: rgba(26, 26, 26, 0.95) !important;
        backdrop-filter: blur(20px);
        border: 1px solid rgba(255, 255, 255, 0.1) !important;
        border-radius: 10px;
    }

    .dark-dropdown .dropdown-item {
        color: var(--modern-text);
        transition: all 0.3s ease;
    }

    .dark-dropdown .dropdown-item:hover {
        background: rgba(0, 212, 255, 0.1);
        color: var(--modern-primary);
    }

    /* Custom Toggler */
    .navbar-toggler {
        padding: 0.5rem;
        border: none;
        width: 40px;
        height: 40px;
        position: relative;
    }

    .toggler-line {
        display: block;
        width: 25px;
        height: 2px;
        background: var(--modern-text);
        margin: 5px 0;
        transition: all 0.3s ease;
        border-radius: 2px;
    }

    .navbar-toggler:focus {
        box-shadow: none;
    }

    .search-toggle {
        background: transparent;
        border: 1px solid rgba(255, 255, 255, 0.2);
        color: var(--modern-text);
        width: 40px;
        height: 40px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.3s ease;
    }

    .search-toggle:hover {
        background: rgba(0, 212, 255, 0.1);
        border-color: var(--modern-primary);
        color: var(--modern-primary);
    }

    /* Floating Social Icons */
    .floating-social {
        position: fixed;
        right: 30px;
        top: 50%;
        transform: translateY(-50%);
        z-index: 1000;
    }

    .social-icon {
        display: block;
        width: 50px;
        height: 50px;
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 50%;
        margin-bottom: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: var(--modern-text);
        text-decoration: none;
        transition: all 0.3s ease;
        backdrop-filter: blur(10px);
    }

    .social-icon:hover {
        transform: translateX(-5px);
        color: white;
    }

    .social-icon.facebook:hover {
        background: #1877f2;
        border-color: #1877f2;
    }

    .social-icon.youtube:hover {
        background: #ff0000;
        border-color: #ff0000;
    }

    .social-icon.instagram:hover {
        background: linear-gradient(45deg, #f09433, #e6683c, #dc2743, #cc2366, #bc1888);
        border-color: #e1306c;
    }

    .social-line {
        width: 1px;
        height: 50px;
        background: linear-gradient(to bottom, transparent, var(--modern-light-gray), transparent);
        margin: 0 auto;
    }
</style>

<!-- Typing Effect Script -->
<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Typing Effect
        const typedElement = document.querySelector('.typed-text');
        const cursorElement = document.querySelector('.cursor');

        if (typedElement) {
            const words = JSON.parse(typedElement.dataset.typed);
            let wordIndex = 0;
            let charIndex = 0;
            let isDeleting = false;

            function typeEffect() {
                const currentWord = words[wordIndex];

                if (isDeleting) {
                    typedElement.textContent = currentWord.substring(0, charIndex - 1);
                    charIndex--;
                } else {
                    typedElement.textContent = currentWord.substring(0, charIndex + 1);
                    charIndex++;
                }

                let typeSpeed = isDeleting ? 50 : 100;

                if (!isDeleting && charIndex === currentWord.length) {
                    typeSpeed = 2000;
                    isDeleting = true;
                } else if (isDeleting && charIndex === 0) {
                    isDeleting = false;
                    wordIndex = (wordIndex + 1) % words.length;
                    typeSpeed = 500;
                }

                setTimeout(typeEffect, typeSpeed);
            }

            typeEffect();
        }

        // Counter Animation
        const counters = document.querySelectorAll('.stat-number[data-count]');
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const counter = entry.target;
                    const target = parseInt(counter.dataset.count);
                    const duration = 2000;
                    const step = target / (duration / 16);
                    let current = 0;

                    const timer = setInterval(() => {
                        current += step;
                        if (current >= target) {
                            counter.textContent = target + (target === 98 ? '%' : '+');
                            clearInterval(timer);
                        } else {
                            counter.textContent = Math.floor(current) + (target === 98 ? '%' : '+');
                        }
                    }, 16);

                    observer.unobserve(counter);
                }
            });
        });

        counters.forEach(counter => observer.observe(counter));
    });
</script>